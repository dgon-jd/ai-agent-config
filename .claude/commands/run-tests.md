---
description: Run pytest with uv — all tests, coverage, or specific categories
argument-hint: [category or flags, e.g. "config", "coverage", "integration"]
allowed-tools: Skill(run-tests), Bash(uv run pytest*)
---

Invoke the run-tests skill for: $ARGUMENTS
